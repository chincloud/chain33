package executor

import (
	"github.com/33cn/chain33/types"
)

// ExecLocal local exec
func (basic *Basic) ExecLocal(tx *types.Transaction, receipt *types.ReceiptData, index int) (*types.LocalDBSet, error) {
	return nil, nil
}
